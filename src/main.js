// 生成全局变量
window.__Mokelay = {
};

// 初始化全局配置

// 加载基础组件库

// 判断目前页面所处状态(编辑状态，渲染状态)

// 判断目前页面所处终端:PC/H5/WX

// 如果是编辑器状态，加载编辑器组件

// 加载自定义组件库，加载完成后，后续还可以还可以动态加载

// 加载内置变量

// 加载内置函数

// 通过URL获取页面参数，获取页面DSL， 目前可以先从dsl/目录里本地加载，方便测试

// 渲染DSL